<Window x:Class="soundapp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sound App - Keyboard Clicker" Height="350" Width="400"
        Background="#1e1e1e"
        WindowStartupLocation="CenterScreen"
        StateChanged="Window_StateChanged"
        Closing="Window_Closing">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="KEYBOARD CLICKER" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Visualizer & Key pressed -->
        <Border Grid.Row="1" x:Name="VisualizerBorder" Background="#333" CornerRadius="10" Margin="0,0,0,20">
            <TextBlock x:Name="KeyDisplay" Text="PRESS ANY KEY" Foreground="#aaa" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <!-- Volume and File Controls -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Volume:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Slider Grid.Column="1" x:Name="VolumeSlider" Minimum="0" Maximum="1" Value="1" VerticalAlignment="Center" ValueChanged="VolumeSlider_ValueChanged"/>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="CurrentFileText" Text="click.wav" Foreground="#ccc" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,10,0"/>
            <Button Grid.Column="1" Content="Browse..." Click="BrowseButton_Click"/>
        </Grid>
    </Grid>
</Window>
